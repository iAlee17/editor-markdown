{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "cargo tauri dev",
            "name": "Tauri Dev",
            "request": "launch",
            "type": "node-terminal",
            "cwd": "${workspaceFolder}"
        },
    ]
}